CHANGELOG
=========
Generated: 2026-03-13
Period: Last 13 days (2026-03-01 – 2026-03-13)

Repositories covered:
  [ptpd]   /Users/alfredo/Workspace/PTP-Implementation/ptpd  (branch: cmake-port)
  [swclock] libraries/swclock  (submodule)

================================================================================
2026-03-13
================================================================================

[ptpd] 2026-03-13 11:49:27  e7807e7  Alfredo Franco
  chore: remove stale docs files

  Deleted six docs/ files that were either superseded by the new
  preset-based build system or covered by inline source documentation:
  BASELINE-CONFIG.txt, iOS-fixes.txt, MACOS-START-CONFIG.txt,
  SLAVE_CONFIGURATION.txt, SWCLOCK_INTEGRATION.txt, PTPBASE-MIB.txt.

--------------------------------------------------------------------------------

[ptpd] 2026-03-13 11:45:21  6d9a49b  Alfredo Franco
  chore: update swclock submodule URL to new repo location

  GitHub repo was moved from aberserk/SwClock to
  alfredo-opensesame/SwClock. Updated .gitmodules to reflect the new
  URL so fresh clones and submodule updates resolve correctly.

--------------------------------------------------------------------------------

[ptpd] 2026-03-13 11:43:20  f825577  Alfredo Franco
  chore: update swclock submodule pointer

  Advances the submodule pointer to the iOS build-fix commit (1791671),
  which guards swclock_event_dump and monitor_demo behind NOT iOS.

--------------------------------------------------------------------------------

[swclock] 2026-03-13 11:43:12  1791671  Alfredo Franco
  fix: guard swclock_event_dump and monitor_demo tools behind NOT iOS

  These executables are not applicable on iOS and cause build failures
  when targeting iOS / iOS-simulator. Both targets are now wrapped in:
    if(NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")

  Also removed trailing whitespace on two nearby lines.

--------------------------------------------------------------------------------

[ptpd] 2026-03-13 11:42:35  3accbe6  Alfredo Franco
  feat: compile built-in leap-seconds table into ptpd library (Option C)

  resources/leap-seconds.list is parsed at build time by
  scripts/tools/gen-leap-header.py into src/def/leap_seconds_builtin.h
  (gitignored, generated by the leap_header custom CMake target).

  timingdomain.c falls back to parseLeapTable() when no runtime
  clock:leap_seconds_file is configured. This fixes the 37-second
  offsetFromMaster seen in apps running on a UTC clock (iOS simulator)
  while PTP timestamps are in TAI.

  Verified on macOS (ptpd-app, offsetFromMaster ~62 us) and iOS simulator
  (PTPMonitor, reached PTP_SLAVE in 2 s, offset ~200 us).

  Changes:
  - scripts/tools/gen-leap-header.py   new build-time generator script
  - resources/leap-seconds.list        extend expiry to 28 December 2028
  - CMakeLists.txt                     add find_package(Python3 REQUIRED)
  - src/CMakeLists.txt                 add leap_header custom target;
                                       add_dependencies for ptpd2,
                                       ptpd-static, ptpd-shared
  - src/timingdomain.c                 add parseLeapTable() helper;
                                       patch both leapFile fallback sites
  - src-gtests/test_leap_builtin.cpp   9 unit tests (all pass)
  - src-gtests/CMakeLists.txt          register test_leap_builtin target
  - .gitignore                         ignore generated header

--------------------------------------------------------------------------------

[ptpd] 2026-03-13 11:05:51  c2ac905  Alfredo Franco
  docs: consolidate DEVELOPMENT.md into BUILD.md, delete DEVELOPMENT.md

  Absorbed all DEVELOPMENT.md content into BUILD.md:
  - Added Project Structure section (source tree + build output table)
  - Expanded VS Code section with full workflow (build, test, clean)
  - Added Configuration Files, swclock Submodule, and Notes for
    Contributors sections
  - Fixed CMake minimum version in Prerequisites: 3.15 -> 3.25
  - Removed DEVELOPMENT.md cross-references from BUILD.md, README.md,
    CONFIGURATION.md
  - Deleted DEVELOPMENT.md (git rm)

--------------------------------------------------------------------------------

[ptpd] 2026-03-13 11:00:29  9134c56  Alfredo Franco
  docs: update all docs for preset-based build system

  Replaced cmake -B build / scripts/build/ references throughout with
  cmake --preset. Updated build output dir names to match new preset
  naming (macos-debug, macos-release, etc.). Corrected option name
  ENABLE_SW_CLOCK -> BUILD_WITH_SWCLOCK. Updated swclock location
  from src/dep/sw_clock/ to libraries/swclock/ submodule.
  Removed stale autotools comparison and migration sections.

--------------------------------------------------------------------------------

[ptpd] 2026-03-13 10:50:41  1d517f8  Alfredo Franco
  cmake: add presets, consumer mode, iOS app fixes, update scripts

  CMakePresets.json with 12 presets (6 library, 6 consumer):
  - Library: macos-debug/release, ios-simulator-debug/release,
    ios-debug/release
  - Consumer: macos-gtest, macos-ptpd-app, iossim-ptpd-app
    (debug + release each)
  - Consumer presets import pre-built ptpd2/swclock via
    PTPD_PREBUILT_DIR

  CMakeLists.txt: PTPD_PREBUILT_DIR consumer mode imports libptpd2.a
  and libswclock.a as STATIC IMPORTED targets with transitive deps
  (zlib, pcap, pthread, math). Gates src-app / src-gtests on
  BUILD_PTPD_APP / BUILD_IOS_APP / BUILD_PTPD_TESTS.

  iOS app: PTPBridge noAdjust flag, PTPManager/ContentView improvements.

  .vscode/tasks.json: 24 configure+build task pairs for all 12 presets.

  scripts/testing: binary path updates; add ptp-ios-run.sh for iOS
  simulator test runs. Remove obsolete scripts/build/ scripts and stale
  resource config variants.

================================================================================
2026-03-12
================================================================================

[ptpd] 2026-03-12 19:29:27  f8223b5  Alfredo Franco
  iOS/macOS: unicast negotiation mode

  resources/ptpd-daemon.conf switched from multicast to unicast with
  negotiation (ip_mode=unicast, unicast_destinations, unicast_negotiation=y).

  PTPBridge: new PTPMode enum (multicast/unicast); init takes mode param.
  - unicast argv: -C -V -L -i <iface> -g -u <masterIP>
  - multicast argv: -C -V -L -i <iface>

  PTPManager: start() takes unicast:Bool, passes PTPMode to bridge.

  ContentView: multicast/unicast toggle (default unicast), master IP
  field shown only in unicast mode, Start disabled when no interface
  selected.

--------------------------------------------------------------------------------

[ptpd] 2026-03-12 18:11:03  a8d901d  Alfredo Franco
  fix: rename PTPD_OPENSESAME_PORTS, fix PCAP filters for non-standard ports

  Renamed macro to PTPD_OPENSESAME_PORTS and made non-privileged ports
  (10319/10320) the build default. Define PTPD_STANDARD_PORTS at
  compile time to revert to 319/320.

  PCAP BPF filters were hardcoded to ports 319/320; replaced with
  snprintf using PTP_EVENT_PORT/PTP_GENERAL_PORT so they track the
  active port at runtime.

  Removed multicast host restriction from the general PCAP filter that
  was silently dropping Delay_Resp (unicast from master to slave IP)
  and leaving oneWayDelay permanently zero.

================================================================================
2026-03-05
================================================================================

[ptpd] 2026-03-05 19:46:43  3acc0bf  Alfredo Franco
  test: add test_system_integration -- live GM performance harness

  GTest suite that calls ptpd_init/ptpd_start/ptpd_shutdown directly
  and measures servo performance against a real PTP grandmaster on
  the LAN. SetUpTestSuite() runs once (120 s), 10 TEST_F methods share
  static s_samples / s_stats.

  Skip conditions (GTEST_SKIP, not FAIL): not root, en5 absent,
  192.168.8.234 unreachable, ptpd_init fails, no GM lock within
  PTPD_TEST_LOCK_TIMEOUT_S seconds.

  Thresholds from real run (2026-03-05): 120/120 locked,
  offset std=127 us, max=1.1 ms, drift=-4.4 ppm,
  MTIE(10s)=891 us, TDEV(1s)=39 us.

  CTest labels: integration (separate from unit tests).
  ctest -L unit       -- fast, no root
  ctest -L integration -- ~180 s, requires root + GM on network

--------------------------------------------------------------------------------

[ptpd] 2026-03-05 19:42:19  ab65cc9  Alfredo Franco
  fix: library_should_exit enables clean ptpd protocol thread shutdown

  ptpd_shutdown() had no stop mechanism for the protocol thread on
  macOS (ios_should_exit only compiled under #ifdef PTPD_IOS),
  causing pthread_join() to hang indefinitely.

  - src/datatypes.h: add volatile int library_should_exit to PtpClock
    (guarded by #ifdef PTPD_LIBRARY_MODE)
  - src/ptpd.c (ptpd_shutdown): set library_should_exit=1 before join
  - src/protocol.c: check library_should_exit in the run-loop exit cond

  Confirmed: ptpd_shutdown() returns promptly in test_system_integration.

--------------------------------------------------------------------------------

[ptpd] 2026-03-05 19:42:05  b63f5fc  Alfredo Franco
  fix: swclock monitor compute thread — 100ms sliced shutdown

  Update swclock submodule to commit 6e50712, which replaces the
  10-second nanosleep in the compute thread idle loop with 100 slices
  of 100 ms, each checking stop_compute_thread.
  Maximum shutdown latency: 10000 ms → 100 ms.

--------------------------------------------------------------------------------

[swclock] 2026-03-05 19:41:55  6e50712  Alfredo Franco
  fix: compute thread — 100ms sliced shutdown instead of 10s nanosleep

  Replaced the single 10-second nanosleep in the compute thread's idle
  loop with 100 slices of 100 ms, each checking stop_compute_thread.
  Maximum shutdown latency: 10000 ms → 100 ms.

--------------------------------------------------------------------------------

[ptpd] 2026-03-05 16:38:45  c38f5fa  Alfredo Franco
  docs: add CHANGELOG, FIXES and TEST_PLAN

  CHANGELOG.txt: version history and notable changes.
  FIXES.txt: record of bug fixes applied to ptpd-app.c macOS port.
  TEST_PLAN.txt: test plan covering GTest suite design,
  ASAN/TSAN/UBSAN strategy, and implementation decisions.

--------------------------------------------------------------------------------

[ptpd] 2026-03-05 16:38:27  30952f4  Alfredo Franco
  scripts: add servo comparison scripts and daemon configs

  resources/: ptpd-daemon-current.conf, ptpd-daemon-option-a.conf,
  ptpd-daemon-option-b.conf — configs for back-to-back servo comparison
  runs.

  scripts/testing/compare-servo-options.sh: run two ptpd-app configs
  back-to-back for N seconds each, with caffeinate, pkill cleanup, and
  timestamped CSV output.

  scripts/testing/summarize-comparison.py: parse CSV logs and print
  per-config offset/drift statistics.

--------------------------------------------------------------------------------

[ptpd] 2026-03-05 16:35:57  11ad64e  Alfredo Franco
  test: add GTest infrastructure and 6 test suites (src-gtests/)

  New BUILD_PTPD_TESTS CMake option (requires BUILD_PTPD_LIBRARY=ON).
  ptpd_gtest() CMake helper links GTest::gtest_main + ptpd_app_utils,
  optionally swclock when BUILD_WITH_SWCLOCK=ON.

  6 test targets (all passing):
  - test_strip_arg (8 cases): flag present/absent, truncation, duplicates
  - test_monitor_csv (6 cases): column count matches header for all
    4 compile-time config combinations
  - test_monotonic_sleep (4 cases): normal 1 s, early-exit via stop flag,
    repeated accumulation, preset stop
  - test_network (5 cases): en5 present, master reachable, ports free
  - test_swclock_rwlock (3 cases): latency <100 ms, concurrent access,
    steady-state after 2 s
  - test_swclock_integration (7 cases): metrics, phase, logging,
    monitoring toggle, adjtime no-op, gettime

--------------------------------------------------------------------------------

[ptpd] 2026-03-05 16:30:58  859f6f0  Alfredo Franco
  refactor: extract strip_arg and monotonic_sleep_1s into ptpd_app_utils

  Moved strip_arg() and the CLOCK_MONOTONIC deadline sleep loop from
  ptpd-app.c into a new translation unit (ptpd_app_utils.c/h) with no
  ptpd or swclock dependencies, allowing GTest targets to link the
  utilities without pulling in the full ptpd daemon.

  monotonic_sleep_1s uses a 50 ms-slice CLOCK_MONOTONIC deadline loop;
  immune to CLOCK_REALTIME step corrections (~-26 s). strip_arg gains
  NUL-termination guarantee (buf[buf_len-1] = '\0').

  src-app/CMakeLists.txt: new ptpd_app_utils STATIC library target.

================================================================================
2026-03-04
================================================================================

[ptpd] 2026-03-04 03:26:04  d4ebcbe  Alfredo Franco
  chore(submodule): bump swclock to PI servo anti-windup fix

  Advances the swclock submodule pointer to commit 0ecc502, which contains
  the PI servo integrator anti-windup fix (see swclock entry below).

--------------------------------------------------------------------------------

[swclock] 2026-03-04 03:14:55  0ecc502  Alfredo Franco
  fix(pi-servo): add integrator anti-windup

  The integrator was accumulating unconditionally before the output clamp,
  causing classic anti-windup failure: when the phase error reached zero the
  inflated integral drove the output hard in the opposite direction, producing
  a continuously growing frequency correction oscillation.

  Fix details:
  - Compute the full PI output first, then decide whether to update the
    integrator.
  - Integrator is only updated when the output is NOT saturated, OR when the
    error is actively driving it away from the clamped limit (back-calculation
    anti-windup pattern).
  - Capture the pre-clamp PI value for accurate frequency-clamp event logging.

--------------------------------------------------------------------------------

[ptpd] 2026-03-04 03:24:34  468880c  Alfredo Franco
  chore(ci): add run_sanitizers.sh script

  New CI script (scripts/testing/run_sanitizers.sh) that runs a full suite of
  static and dynamic analysis checks against the ptpd source:
  - AddressSanitizer (ASAN) build and run
  - ThreadSanitizer (TSAN) build and run
  - UndefinedBehaviorSanitizer (UBSAN) build and run
  - clang-tidy static analysis (scoped to src/ only)
  - clang-format style check (scoped to src/ only)

  Notable implementation details:
  - Bash 3.2 compatible (macOS default shell).
  - OS detection sets correct sanitizer flags (e.g. detect_leaks=0 on macOS
    since LeakSanitizer is not supported on the Darwin kernel).
  - Passes macOS SDK sysroot to clang-tidy to resolve system header paths.
  - All checks are scoped to src/ only (excludes dep/iniparser and other
    third-party code).

--------------------------------------------------------------------------------

[ptpd] 2026-03-04 03:23:31  984a53e  Alfredo Franco
  style: apply LLVM C formatting to src/ via clang-format

  Added .clang-format configuration file at repository root:
  - Style:         LLVM
  - Language:      C
  - SortIncludes:  Never  (preserves required header dependency ordering)

  Reformatted all .c and .h files under src/ (excluding dep/iniparser which
  is third-party code). Include order is intentionally preserved because ptpd
  has headers that must be included in a specific sequence for correct
  compilation.

--------------------------------------------------------------------------------

[ptpd] 2026-03-04 03:22:38  c8758c1  Alfredo Franco
  fix(build): guard keyid_t typedef for macOS compatibility

  macOS system headers do not define keyid_t (an NTP internal type). Two
  headers were affected:
  - src/dep/ntp_isc_md5.h
  - src/dep/ntpdcontrol.h

  Added preprocessor guards around the typedef so the definition appears
  exactly once regardless of include order, fixing "unknown type name
  'keyid_t'" clang errors on macOS builds.

================================================================================
2026-03-03
================================================================================

[ptpd] 2026-03-03 22:03:30  2f6a7b5  Alfredo Franco
  docs: add USAGE.md with library integration guide and test runner docs

  New USAGE.md document covering:
  - Library integration via CMake: add_subdirectory and pre-built library
    linking patterns, relevant CMake options.
  - Public API reference: ptpd_init(), ptpd_start(), ptpd_shutdown(),
    ptpd_gettime().
  - Full PTP configuration reference tables for sections: ptpengine, clock,
    servo, global, ntpengine.
  - Common configuration presets:
      * Slave-only multicast
      * Slave-only unicast
      * Master-only
      * Master-slave (auto)
  - Lock file behaviour and ignore_lock guidance.
  - macOS privilege notes: PTPD_NO_ROOT_CHECK, PTPD_OPENSESAME_PORTS, required
    sysctl knobs.
  - PTPD_IOS flag explanation and when it applies on macOS.
  - swclock backend overview.
  - Redistribution guidance: BSD 2-Clause compliance, pkg installer and
    LaunchDaemon packaging.
  - ptp-app-run.sh test runner: step-by-step operational description.

--------------------------------------------------------------------------------

[ptpd] 2026-03-03 17:11:50  3ec89d2  Alfredo Franco
  ci: remove dead Ubuntu install step and redundant os condition

  Cleaned up .github/workflows/ci.yml: removed an install step that was
  no longer reachable and an os condition check that had become redundant
  after Ubuntu was dropped from the build matrix.

--------------------------------------------------------------------------------

[ptpd] 2026-03-03 17:08:27  445db53  Alfredo Franco
  ci: remove ubuntu-latest from build matrix

  Ubuntu builds were consistently failing due to Linux platform differences
  not yet addressed in this branch. Removed ubuntu-latest from the CI matrix
  to keep the pipeline green while Linux support is worked on separately.

--------------------------------------------------------------------------------

[ptpd] 2026-03-03 17:04:28  78bc9d5  Alfredo Franco
  submodule: swclock Linux build fix

  Advances the swclock submodule pointer to commit 7c5d93e, which contains
  the Linux build portability fixes (see swclock entry below).

--------------------------------------------------------------------------------

[swclock] 2026-03-03 17:04:26  7c5d93e  Alfredo Franco
  fix: Linux build errors - add _GNU_SOURCE and fix tm_zone portability

  Two portability fixes for Linux/glibc builds:

  1. CMakeLists.txt: add _GNU_SOURCE definition for non-Apple builds.
     Required to make clockid_t, pthread_rwlock_t, CLOCK_REALTIME and
     CLOCK_MONOTONIC visible from glibc headers.

  2. sw_clock_utilities.c: tm_zone is a BSD/macOS extension absent from
     Linux's struct tm. Replaced direct tm_zone access with strftime %Z
     for portable timezone abbreviation retrieval.

--------------------------------------------------------------------------------

[ptpd] 2026-03-03 16:59:30  18ad175  Alfredo Franco
  ci: add submodules: recursive to checkout step

  BUILD_WITH_SWCLOCK=ON is now the default (see commit aa3c23c). The swclock
  submodule must therefore be initialised during CI checkout or the build
  will fail. Added `submodules: recursive` to the actions/checkout step in
  .github/workflows/ci.yml.

--------------------------------------------------------------------------------

[ptpd] 2026-03-03 16:51:02  b251c64  Alfredo Franco
  submodule: update swclock to cleanup commit (f8c4ca4)

  Advances the swclock submodule pointer to the cleanup commit that removes
  v1 backup files (see swclock entry below).

--------------------------------------------------------------------------------

[swclock] 2026-03-03 16:50:41  f8c4ca4  Alfredo Franco
  cleanup: remove v1 backups and redundant v2 snapshot files

  sw_clock.c and sw_clock_constants.h already contain the complete v2
  implementation. The following files were removed as dead code:
  - sw_clock_v2.c
  - sw_clock_constants_v2.h
  - sw_clock.c.bak
  - sw_clock.c.bak2
  - sw_clock.c.bak3
  - swclock_jsonld.c.backup

--------------------------------------------------------------------------------

[ptpd] 2026-03-03 16:47:53  aa3c23c  Alfredo Franco
  cmake: enable BUILD_WITH_SWCLOCK=ON by default

  SwClock v2 is now the recommended and default clock backend for ptpd.
  Changes:
  - CMakeLists.txt: option(BUILD_WITH_SWCLOCK) default changed OFF -> ON.
  - scripts/build/config-cmake-debug.sh: corrected flag name from
    ENABLE_SW_CLOCK (old/wrong) to BUILD_WITH_SWCLOCK.
  - scripts/build/config-cmake-release.sh: added explicit
    -DBUILD_WITH_SWCLOCK=ON.
  - docs/SWCLOCK_INTEGRATION.txt: updated example CMake invocation snippet
    to reflect the new default and correct flag name.

--------------------------------------------------------------------------------

[ptpd] 2026-03-03 16:45:18  494957f  Alfredo Franco
  submodule: update swclock to v2 improvements (033bef5)

  Advances the swclock submodule pointer to commit 033bef5, which contains
  the SwClock v2 performance improvements (see swclock entry below).

--------------------------------------------------------------------------------

[swclock] 2026-03-03 16:44:49  033bef5  Alfredo Franco
  perf: SwClock v2 improvements and performance documentation

  Five targeted improvements to the PI servo and clock discipline algorithm:

  1. Remove MIN_SLEW_PPM floor (was 100 ppm): the artificial floor caused a
     persistent +600 µs offset bias in steady-state tracking.

  2. Preserve integrator (pi_int_error_s) across ADJ_OFFSET calls: previously
     the integrator was reset on every offset adjustment, preventing steady-
     state error elimination.

  3. Fix gettime race condition: sample CLOCK_MONOTONIC_RAW before acquiring
     the rwlock to eliminate the time consumed waiting for the lock from the
     interpolated timestamp.

  4. Two-stage PI gains:
     - Pull-in mode  (|error| > 100 µs): Kp=200, Ki=8  — fast convergence.
     - Tracking mode (|error| ≤ 100 µs): Kp=20,  Ki=1  — low steady-state
       overshoot.

  5. Persist learned frequency offset to /tmp/swclock.freq_state across
     restarts, avoiding the initial frequency ramp-up on every invocation.

  Measured results (5-minute live PTP sessions, interface en5, 1000BASE-T):
    SwClock v2 vs v1:  offset bias -35%, RMS -31%, peak-to-peak -9%
    SwClock v2 vs sys: 100% sub-1ms samples (vs 57.9%), bias -79%, RMS -77%

  Adds docs/PERFORMANCE.md with full measurement methodology and result tables.

================================================================================
2026-03-01
================================================================================

[ptpd] 2026-03-01 22:52:07  8d88273  Alfredo Franco
  Add ptp-app-run.sh and fix ptpd-daemon.conf for multicast slave sync

  resources/ptpd-daemon.conf:
  - Set ip_mode=multicast and transport=ipv4.
  - Replaced invalid step_threshold option with step_startup and
    step_startup_force (correct option names for this ptpd version).
  - Added drift_handling=reset.
  - Set interface=en5.

  scripts/testing/ptp-app-run.sh:
  - New script: copy of ptpd-run.sh specialised for the ptpd-app binary.
  - Fixed PTP_PAT regex to match ptpd-app process name (dash vs underscore
    in the binary name caused the stale-process kill logic to fail).
  - Added port-based force-kill fallback: if the pattern match fails,
    detect and kill any process still holding UDP ports 319/320.

================================================================================
END OF CHANGELOG
================================================================================
